[build]
  command = "npm run build"
  publish = "dist"

[functions]
  node_bundler = "esbuild"

# SPA fallback — serve index.html for all non-asset routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
